# This file is overwritten at build time by release.yml using the DEPLOY_HOST secret.
# Do not hardcode real IPs here — this file is committed to the repository.
CVE_CORE_HTTP_HOST = "SERVER_IP"
CVE_CORE_HTTP_PORT = 8080
